Wilbur Dekum sketch icon set v1 (18 icons)
- 48x48 viewBox, 2px stroke, round caps/joins, stroke-only
- Strokes use currentColor: set the CSS color on the parent to recolor
- generate_icons.py reproduces the set deterministically; add new icons there
  so the wobble stays uniform across the system
